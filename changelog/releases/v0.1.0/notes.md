This inaugural release introduces the `pi-tenzir` package so Tenzir users can install Tenzir workflows in Pi with one command. After installation, `/skill:tenzir` activates the Tenzir skill, and Pi automatically fetches and registers the latest skill version on first use.

## 🚀 Features

### Inaugural release of the Pi Tenzir package

This inaugural release introduces the `pi-tenzir` package so Tenzir users can install and use Tenzir-focused capabilities in Pi right away.

*By @mavam and @codex.*
